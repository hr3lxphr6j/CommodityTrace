spring:
  datasource:
    username: root
    password: 8681700
    url: jdbc:mysql://127.0.0.1:3306/commodity_trace
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    database: mysql
  jackson:
    serialization: true
  output:
    ansi:
      enabled: always
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0
server:
  context-path: /api/v1
logging:
  level: debug